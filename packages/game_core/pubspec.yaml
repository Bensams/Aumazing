name: game_core
description: Flame game logic, game registry, and shared game configuration for Aumazing.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.7.2
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.30.1
  shared_ui:
    path: ../shared_ui

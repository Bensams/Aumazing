name: shared_audio
description: Audio helpers, volume settings, and sound services for Aumazing.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.7.2
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^6.1.0

flutter:
  assets:
    - assets/audio/
